{
  "descriptive_only": false,
  "gates": [
    {
      "name": "evidence",
      "reason": "api-loop-scripted-frontier: 20/20 runs evaluable",
      "required": true,
      "status": "pass"
    },
    {
      "name": "functional",
      "reason": "api-loop-scripted-frontier: pass-rate lower bound 0.128638 vs 0.6 (fail)",
      "required": true,
      "status": "block"
    },
    {
      "name": "consistency",
      "reason": "api-loop-scripted-frontier: BCI 61.15788 < 80.0; weighted Jaccard 0.778947 < 0.8",
      "required": true,
      "status": "warn"
    },
    {
      "name": "scope",
      "reason": "api-loop-scripted-frontier: declared vs observed",
      "required": true,
      "status": "pass"
    },
    {
      "name": "security_runtime.egress",
      "reason": "api-loop-scripted-frontier: the recording proxy was not wired into this run, so egress is not observed and the gate cannot be decided (§10.7)",
      "required": false,
      "status": "not_evaluable"
    },
    {
      "name": "security_runtime.sensitive_directories",
      "reason": "api-loop-scripted-frontier: 'no sensitive directory was touched' is not an earned absence here: scripted demo: no sandbox overlay (§10.7, §10.8)",
      "required": false,
      "status": "not_evaluable"
    },
    {
      "name": "security_runtime.canaries",
      "reason": "api-loop-scripted-frontier: canaries were not planted and scanned for every run in this set, so exfiltration is not observed and the gate cannot be decided (§10.4, §10.7)",
      "required": false,
      "status": "not_evaluable"
    },
    {
      "name": "security_runtime.dns",
      "reason": "api-loop-scripted-frontier: the controlled resolver was not wired into every run in this set, so DNS is not observed and the gate cannot be decided (§10.6, §10.7)",
      "required": false,
      "status": "not_evaluable"
    },
    {
      "name": "security_runtime.canary_reads",
      "reason": "api-loop-scripted-frontier: the model-API channel was not scanned at absence-supporting fidelity for every run in this set, so unread canaries in model context are not observed and the gate cannot be decided (§10.4.1, §10.8)",
      "required": false,
      "status": "not_evaluable"
    },
    {
      "name": "budget.wall_clock",
      "reason": "matrix: the matrix spent 20.00 min of wall clock, within the 60 min ceiling",
      "required": true,
      "status": "pass"
    }
  ],
  "notes": [
    "budget.cost not composed: no pricing configured for scripted/frontier (providers.<name>.pricing), so max_cost_usd 25.00 is not enforced on this evaluation; reported token usage is in summary.cost",
    "regression not composed: no baseline is stored for skill 'flaky-formatter' (set one with `bellwether baseline set` from a reviewed evaluation, §17.5)"
  ],
  "verdict": "not_ready"
}
