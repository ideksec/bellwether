Type: Added
Summary: Export reports as JSON via `report --format json`.

Adds a machine-readable output alongside the existing Markdown so downstream tools can
consume verdicts without scraping the rendered page.
